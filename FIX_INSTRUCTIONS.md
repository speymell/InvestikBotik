# 🔧 Инструкция по исправлению демо-режима на телефоне

## Что было сделано

Добавлен **принудительный механизм** выхода из демо-режима при обнаружении Telegram Web App.

## Что нужно сделать СЕЙЧАС

### Шаг 1: Задеплойте изменения
```bash
git add .
git commit -m "fix: принудительный выход из демо при входе через Telegram"
git push
```

### Шаг 2: Дождитесь деплоя на Render.com
- Зайдите на https://dashboard.render.com
- Дождитесь завершения деплоя (обычно 2-3 минуты)

### Шаг 3: Откройте приложение на телефоне через Telegram
1. Откройте Telegram на телефоне
2. Найдите вашего бота
3. Откройте Web App

### Что произойдёт автоматически:
- ✅ Скрипт обнаружит, что вы залогинены как демо-пользователь
- ✅ Автоматически выполнит выход (очистит сессию)
- ✅ Залогинит вас через ваш Telegram аккаунт
- ✅ Вы увидите своё имя вместо "Демо пользователь"

### Если автоматически не сработало:
1. Нажмите кнопку **"Выйти"** в нижней панели навигации (она появляется только для демо-пользователей)
2. Откройте приложение снова через Telegram

### Как проверить, что всё работает:
- В верхней части приложения должно быть ваше имя из Telegram, а не "Демо пользователь"
- Кнопка "Выйти" должна исчезнуть из навигации
- В консоли браузера (если откроете DevTools) будет сообщение: "🔐 Автологин через Telegram"

## Технические детали

### Что было изменено:
1. **`routes.py`**: Добавлен endpoint `/logout` и улучшена логика `/demo_login`
2. **`layout.html`**: Добавлен принудительный выход при обнаружении демо + Telegram
3. **`index.html`**: Кнопка "Попробовать демо" скрывается в Telegram
4. Добавлены кнопки "Выйти" в навигацию (десктоп и мобильная)

### Почему это работает:
- При каждой загрузке страницы проверяется флаг `data-is-demo`
- Если обнаружен Telegram Web App И демо-режим, выполняется:
  1. `fetch('/logout')` - очистка сессии
  2. Редирект на `/login?telegram_id=...` - вход через Telegram
- Это происходит **автоматически** при каждой загрузке

## Если проблема сохраняется

Попробуйте:
1. Закрыть Web App полностью в Telegram
2. Перезапустить Telegram
3. Открыть Web App снова

Или очистите кэш браузера Telegram (в настройках Telegram → Данные и память → Очистить кэш)
