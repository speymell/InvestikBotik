# Исправление демо-режима

## Проблема
Кнопка "Попробовать демо" была доступна всегда, даже при входе через Telegram. После нажатия устройство запоминало демо-пользователя и не переключалось на Telegram-авторизацию. Даже после обновления кода, старая сессия оставалась активной.

## Решение

### 1. **Скрытие кнопки демо в Telegram** (`templates/index.html`)
- Кнопка "Попробовать демо" теперь скрывается автоматически при обнаружении Telegram Web App
- Проверка происходит на клиенте через JavaScript

### 2. **Автоматическое переключение с демо на Telegram** (`templates/layout.html`)
- Добавлен флаг `data-is-demo` в body для отслеживания демо-режима
- При обнаружении Telegram Web App и демо-пользователя происходит автоматический редирект на Telegram-авторизацию
- Используется серверный флаг `is_demo_user` для точной проверки

### 3. **Блокировка демо-входа из Telegram** (`routes.py`)
- Добавлена проверка User-Agent в `/demo_login`
- Запрещён демо-вход из Telegram Web App
- При входе через Telegram флаг `is_demo` очищается
- При демо-входе флаг `is_demo` устанавливается в `True`

### 4. **Context Processor** (`routes.py`)
- Добавлен `inject_demo_flag()` для передачи флага `is_demo_user` во все шаблоны
- Флаг доступен во всех Jinja2 шаблонах

### 5. **Принудительный выход из демо** (`routes.py`, `templates/layout.html`)
- Добавлен endpoint `/logout` для очистки сессии
- При обнаружении демо-пользователя в Telegram происходит:
  1. Вызов `/logout` через fetch для очистки сессии
  2. Автоматический редирект на Telegram-авторизацию
- Добавлена кнопка "Выйти" в навигацию для демо-пользователей (десктоп и мобильная версия)

## Результат
- ✅ Кнопка "Попробовать демо" скрыта для пользователей Telegram
- ✅ **ПРИНУДИТЕЛЬНОЕ** переключение с демо на Telegram-авторизацию при каждой загрузке страницы
- ✅ Невозможно войти в демо-режим из Telegram Web App
- ✅ Приоритет всегда у Telegram-авторизации
- ✅ Кнопка "Выйти" для ручного выхода из демо-режима

## Как исправить застрявшую демо-сессию
Если вы уже залогинены как демо-пользователь на телефоне:

**Автоматически (рекомендуется):**
1. Просто откройте приложение через Telegram Web App
2. Скрипт автоматически обнаружит демо-режим
3. Выполнит выход и залогинит через ваш Telegram аккаунт

**Вручную:**
1. Нажмите кнопку "Выйти" в навигации (появляется только для демо-пользователей)
2. Откройте приложение снова через Telegram

## Тестирование
1. Откройте приложение в обычном браузере - кнопка "Попробовать демо" видна
2. Откройте через Telegram Web App - кнопка скрыта
3. Войдите в демо через браузер, затем откройте через Telegram - автоматически выйдет и переключится на ваш Telegram аккаунт
4. Проверьте консоль браузера - должно быть сообщение "🔄 Обнаружен демо-пользователь в Telegram! Принудительный выход..."
